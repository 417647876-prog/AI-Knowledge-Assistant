# 阶段 3 执行进度

> 这是第三阶段唯一进度入口。开始和完成每个 Task 都必须更新本文件；计划文档中的复选框用于任务细节，本文件用于总览。

## 当前状态

| 项目 | 当前值 |
|---|---|
| 总体状态 | 阻塞 |
| 总进度 | 15 / 25 Task |
| 当前阶段 | 3C：Rerank 重排序（质量门未通过） |
| 当前 Task | 3C.1 生产安全增强已完成真实校准与回归；原 3C Task 5 质量门仍未通过 |
| 推荐模型 | Sol｜高推理 |
| 实际模型 | Sol｜高推理，按 Task 5 的真实 CPU BGE 验收边界执行 |
| 最近验证 | 3C.1：275 passed / 60 skipped；临时空库从零迁移后 60 个 integration passed，finally 删除确认剩余 0；Ruff check 通过；143 个文件 format check 通过；阈值评估 MRR@5 相对 3B 仍提升 0.00% |
| 当前阻塞 | MRR@5 未达到相对 3B 提升至少 5% 的门槛；Task 5 与 3C 不得标完成 |
| 下一步 | 先修正 3C 无法处理误召回和零候选的根因，再重跑同一质量门；不得开始 3D |
| 最后更新 | 2026-07-15，3C.1 接受门完成真实 CPU 校准、固定 30 条回归与空库集成验证；原 Task 5 质量门仍未通过，3D 保持阻塞 |

## 阶段总览

| 阶段 | 状态 | 进度 | 质量门 | 计划 |
|---|---|---:|---|---|
| 3A 评估基线 | 已完成 | 5 / 5 | 30 条中文数据并生成纯向量基线 | [3A 计划](superpowers/plans/2026-07-15-stage-3a-evaluation-baseline.md) |
| 3B 混合检索 | 已完成 | 6 / 6 | 关键词保持 100%；总体 Recall、引用和拒答均不低于纯向量 | [3B 计划](superpowers/plans/2026-07-15-stage-3b-hybrid-retrieval.md) |
| 3C Rerank | 阻塞 | 4 / 5 | MRR@5 提升 ≥ 5%，引用命中率不下降；当前 MRR 提升 0.00% | [3C 计划](superpowers/plans/2026-07-15-stage-3c-reranking.md) |
| 3D Query Rewrite | 阻塞 | 0 / 5 | 3C 未通过，不得开始；通过后仍需检查 2D 同步门禁 | [3D 计划](superpowers/plans/2026-07-15-stage-3d-query-rewrite.md) |
| 3E 综合验收 | 未开始 | 0 / 4 | Recall@5 ≥ 85%，引用和拒答准确率 ≥ 90% | [3E 计划](superpowers/plans/2026-07-15-stage-3e-quality-acceptance.md) |

## 3A：评估数据集与质量基线

- [x] Task 1：评估契约与数据集加载 — `Terra`
- [x] Task 2：检索质量指标 — `Terra`
- [x] Task 3：评估 Runner — `Sol`
- [x] Task 4：纯向量基线 CLI 与数据库验证 — `Terra`
- [x] Task 5：阶段验收与进度更新 — `Sol`

## 3B：PostgreSQL 混合检索

- [x] Task 1：确定性中文检索 Token — `Terra`
- [x] Task 2：检索字段、迁移与入库写入 — `Sol`
- [x] Task 3：关键词 Retriever — `Sol`
- [x] Task 4：RRF 与 HybridRetriever — `Sol`
- [x] Task 5：配置与 RagService 接线 — `Sol`
- [x] Task 6：3B 指标验收 — `Sol`

## 3C：Rerank 重排序

- [x] Task 1：Reranker 契约与稳定排序 — `Terra`
- [x] Task 2：Fake 与本地 BGE Reranker — `Terra`
- [x] Task 3：配置和依赖构造 — `Terra`
- [x] Task 4：RagService 候选重排序与回退 — `Sol`
- [ ] Task 5：3C 指标与性能验收 — `Sol`（真实报告已生成，质量门未通过）

## 3D：Query Rewrite 与多轮问题改写

- [ ] Task 1：阶段 2D 集成门禁 — `Sol`
- [ ] Task 2：选择性改写规则 — `Terra`
- [ ] Task 3：RagService 回退和流式事件 — `Sol`
- [ ] Task 4：流式 API 与前端展示回归 — `Terra`
- [ ] Task 5：3D 评估与验收 — `Sol`

执行前置：阶段 3 分支必须包含阶段 2D 的 `ConversationMessage`、`QuestionRewriter`、流式问答接口和前端 `buildHistory`。2D 使用浏览器 `sessionStorage` 历史并在请求中发送受限 history，不使用服务端会话表；3D 不得重复实现持久化历史。

## 3E：综合评估与验收

- [ ] Task 1：报告比较与兼容性校验 — `Terra`
- [ ] Task 2：中文报告与性能说明 — `Terra`
- [ ] Task 3：比较命令与自动退出码 — `Sol`
- [ ] Task 4：最终验证、演示与收口 — `Sol`

## 状态更新规则

1. 开始 Task：总体和阶段改为 `进行中`，填写实际模型。
2. 验证失败：保持 `进行中`；无法安全继续时改为 `阻塞` 并记录原因。
3. 验证通过：勾选 Task，增加完成数，记录命令、结果和提交标识。
4. 阶段全部 Task 与质量门通过：阶段改为 `已完成`，下一阶段才能开始。
5. Terra 在同一验证上连续失败两次：停止试错，记录原因并切换 Sol。
6. 计划编写、文档确认和模型讨论不计入功能 Task 完成数。

## 验证证据

| 日期 | 阶段/Task | 命令或报告 | 结果 | 提交 |
|---|---|---|---|---|
| 2026-07-15 | 计划准备 | 第三阶段总体设计和 3A～3E 计划 | 文档已生成，功能尚未执行 | 设计提交 `814114e` |
| 2026-07-15 | 3A Task 1 | `uv run pytest tests/unit/test_evaluation_dataset.py -q`；Ruff check/format | 4 passed，检查通过 | `10aab7c` |
| 2026-07-15 | 3A Task 2 | `uv run pytest tests/unit/test_evaluation_dataset.py tests/unit/test_evaluation_metrics.py -q`；Ruff check/format | 13 passed，检查通过 | `9c02a6f` |
| 2026-07-15 | 3A Task 3 | `uv run pytest tests/unit/test_evaluation_dataset.py tests/unit/test_evaluation_metrics.py tests/unit/test_evaluation_runner.py -q`；Ruff check/format | 16 passed，检查通过 | `87c94af` |
| 2026-07-15 | 3A Task 4（自动验证） | 23 个评估单测；`RUN_DATABASE_TESTS=1` 的 1 个集成测试；Ruff；CLI `--help`；只读数据库连接 | 全部通过；修复 Windows Proactor 与 psycopg 不兼容 | `dbde5b1`、`cbb57e9` |
| 2026-07-15 | 3A Task 4（正式基线） | `uv run python -m scripts.evaluate_rag --dataset tests/fixtures/evaluation/stage3.jsonl --knowledge-base-id f1279eb3-dcfd-490e-ad33-973e23df5e5e --mode vector --output reports/stage3a-vector-baseline.json` | 30 条案例；Recall@5、MRR@5、引用命中率、拒答准确率均为 83.33%；P50 22.05 ms，P95 40.18 ms | `fcabad7` |
| 2026-07-15 | 3A Task 5 | `uv run pytest -q`；Ruff check/format；临时空库迁移；`RUN_DATABASE_TESTS=1` 全部集成测试 | 170 passed、53 个集成测试 passed、Ruff check 通过、122 个文件格式通过；Sol 审查无阻塞问题 | `23457f3` |
| 2026-07-15 | 3B Task 1 | `uv run pytest tests/unit/test_search_tokens.py tests/unit/test_text_cleaning.py -q`；`uv run ruff check`；`uv run ruff format --check`；`git diff --check` | 5 passed；Ruff check/format 通过；新增确定性 Token 纯函数：ASCII 字母数字小写、中文相邻双字、单汉字保留、首次出现去重、空文本返回空字符串 | `6c4086b` |
| 2026-07-15 | 3B Task 2 | `uv run pytest tests/unit -q`；`RUN_DATABASE_TESTS=1` 运行数据库结构与向量入库测试；`uv run alembic downgrade 20260714_04` 后重新升级；Ruff check/format | 174 个单元测试通过；6 个相关集成测试通过；旧片段保留且可由重处理补齐 Token；开发库最终为 `20260715_05 (head)` | `f79c8e0` |
| 2026-07-15 | 3B Task 3 | `uv run pytest tests/unit -q`；`RUN_DATABASE_TESTS=1` 运行关键词、向量和权限集成测试；Ruff check/format | 174 个单元测试通过；8 个集成测试通过；精确编号、双 Token 排序、同分稳定顺序、top_k、空查询和跨知识库隔离均通过 | `9fad780` |
| 2026-07-15 | 3B Task 4 | `uv run pytest tests/unit/test_reciprocal_rank_fusion.py tests/unit/test_hybrid_retriever.py tests/unit/test_evaluation_runner.py tests/unit/test_rag_service.py -q`；Ruff check/format | 13 passed；RRF 去重、双路加分、稳定排序、top_k、双路调用和评估 query 透传通过 | `444a78a` |
| 2026-07-15 | 3B Task 5 | `RUN_DATABASE_TESTS=1` 运行配置、RagService、问答 API 与权限测试；Ruff check/format | 32 passed；默认 vector、显式 hybrid、RRF 常数范围、普通与流式 query 透传及权限回归通过 | `79982e9` |
| 2026-07-15 | 3B Task 6（未通过） | 生成 `stage3b-vector.json` 与 `stage3b-hybrid.json`；`uv run pytest -q`；临时空库运行全部集成测试；Ruff check/format | 修复共享 `AsyncSession` 并发查询；总体 Recall@5 83.33% → 86.67%；关键词 100% → 100%，未提升 10 个百分点；引用 83.33% → 80%；181 passed、59 个集成测试 passed、132 个文件格式通过 | 混合评估修复 `adf0be6` |
| 2026-07-15 | 3B Task 6（修复后通过） | 逐案例对比 vector/hybrid；TDD 验证低覆盖率关键词候选；重新生成两份报告；`uv run pytest -q`；临时空库迁移并运行全部集成测试；Ruff check/format | 过滤低于 20% 查询 Token 覆盖率的关键词候选；vector 四项指标均为 83.33%，hybrid 四项指标均为 93.33%；关键词 Recall@5 100% → 100%，满足上限感知质量门；181 passed、60 个集成测试 passed、132 个文件格式通过 | 实现修复 `1d37465`；验收文档本提交 |
| 2026-07-15 | 3C Task 1 | `uv run pytest tests/unit/test_reranking.py -q`；Ruff check/format；`git diff --check` | 6 passed；Reranker 协议、批量评分数量校验、稳定排序、分数替换与 Top K 参数校验通过 | 本提交 |
| 2026-07-15 | 3C Task 2 | `uv run pytest tests/unit/test_reranker_providers.py -q`；Ruff check/format；`git diff --check` | 6 passed；Fake 固定/递减分数、本地 CrossEncoder 惰性加载、设备解析、线程推理、空输入与脱敏错误通过；测试未下载模型 | 本提交 |
| 2026-07-15 | 3C Task 3 | `uv run pytest tests/unit/test_config.py tests/unit/test_question_reranker_dependency.py -q`；Ruff check/format；`git diff --check` | 19 passed；默认禁用、配置范围、候选数跨字段约束和 disabled/fake/local 依赖选择通过 | 本提交 |
| 2026-07-15 | 3C Task 4 | `uv run pytest tests/unit/test_rag_service.py tests/integration/test_question_api.py tests/integration/test_resource_permissions.py -q`；显式 `RUN_DATABASE_TESTS=1` 补跑；Ruff check/format；`git diff --check` | 原命令 11 passed、15 skipped；显式数据库模式 26 passed；禁用兼容、候选重排、严格失败、精确回退、日志脱敏与 API 装配通过 | 本提交 |
| 2026-07-15 | 3C Task 5（初次测量无效） | 初次 `--mode rerank` 报告 | Runner 使用独立 raw hybrid Top5 计算 MRR 和延迟，未测量最终重排链路；旧 MRR/P50/P95 作废 | `2ad1a64` |
| 2026-07-15 | 3C Task 5（Critical 修复后未通过） | 单次最终链路：embedding → hybrid candidate_k=20 → 真实本地 `BAAI/bge-reranker-base` → Top5；指标、引用和延迟均取最终 chunks；全量测试、临时空库全部 integration、Ruff check/format、`git diff --check` | 3B/3C 的 MRR@5 均为 93.33%，相对提升 0.00%，未达到 5%；引用均为 93.33%；3C CPU P50 62.77 ms、P95 106.07 ms；13 个 Critical 聚焦测试、216 个全量测试、60 个数据库集成测试通过 | 本次修复提交 |
| 2026-07-15 | 3C Task 5（最终安全修复，仍未通过质量门） | 非法分数与 fallback、同实例推理锁、评测安全环境、流式重排及同 key 首次 cache miss 原子初始化回归；Ruff check/format；`git diff --check` | 首轮 45 个聚焦测试、全量 224 passed / 60 skipped；cache/fallback/流式聚焦 17 passed；本轮尚未重新运行 `RUN_DATABASE_TESTS=1`，60 个 integration passed 证据来自 `336ea70` 之前的完整门；MRR@5 提升仍为 0.00%，Task 5 与 3C 保持未完成，3D 阻塞 | 首轮安全修复 `4661e05`；cache miss 修复本提交 |
| 2026-07-15 | 3C 主控最终复验（质量门仍未通过） | `uv run pytest -q`；Ruff check/format；`git diff --check`；创建临时空库并从零升级到 Alembic head；`RUN_DATABASE_TESTS=1` 运行全部 integration；finally 删除临时库 | 225 passed / 60 skipped；60 个数据库集成测试 passed；Ruff check 通过；139 个文件 format check 通过；最终全分支复审无 Critical/Important/Minor；MRR@5 相对提升仍为 0.00%，不改变阻塞结论 | 本提交 |
| 2026-07-15 | 3C.1 Task 5（接受门真实验收） | 独立 24 条校准集；真实 CPU `BAAI/bge-reranker-base`；临时阈值重跑固定 30 条；逐案例核对接受数量；临时空库从零迁移并运行全部 integration；全量 pytest、Ruff check/format、`git diff --check` | 275 passed / 60 skipped；校准 FAR=0、正样本接受率=83.33%、建议阈值=0.4118897467851639；固定评估四项均 93.33%，P50=62.16 ms、P95=101.21 ms，30/30 接受数量一致；相对 3B MRR 提升 0.00%；60 integration passed，临时库删除后剩余 0；143 个文件格式通过 | 本提交 |

## 模型切换记录

| 日期 | Task | 原模型 | 新模型 | 原因 |
|---|---|---|---|---|
| 2026-07-15 | 3A Task 2 → Task 3 | Terra｜中推理 | Sol｜高推理 | Runner 涉及异步协议、指标汇总和可复现报告契约 |
| 2026-07-15 | 3A Task 3 → Task 4 | Sol｜高推理 | Terra｜中推理 | CLI 装配、测试和数据库隔离验证可由中等推理完成 |
| 2026-07-15 | 3A Task 4 → Task 5 | Terra｜中推理 | Sol｜高推理 | 阶段验收需要完整质量门、跨文件审查和后续阶段决策 |
| 2026-07-15 | 3B Task 1 → Task 2 | Terra｜中推理 | Sol｜高推理 | Task 2 涉及 SQLAlchemy 模型、Alembic 迁移、入库路径与数据库升降级验证 |

## 3A 纯向量基线结果

- 评估知识库：`f1279eb3-dcfd-490e-ad33-973e23df5e5e`，由 5 份测试资料入库，共 13 个向量片段。
- 本地报告：`backend/reports/stage3a-vector-baseline.json`（包含运行数据，已被 Git 忽略）。
- 总体：Recall@5 = 83.33%，MRR@5 = 83.33%，引用命中率 = 83.33%，拒答准确率 = 83.33%。
- 分类 Recall@5：关键词 100%，语义 100%，拒答 83.33%，多轮 50%，干扰 75%。
- 该结果是 3B～3D 的对照基线；尤其 3D 必须以多轮 50% 为起点验证改写收益。

## 3B 混合检索验收结果

- 本地报告：`backend/reports/stage3b-vector.json` 与 `backend/reports/stage3b-hybrid.json`，均被 Git 忽略。
- 上限感知质量门：关键词 Recall@5 必须达到 `min(100%, 纯向量关键词 Recall@5 + 10 个百分点)`；总体 Recall@5、引用命中率和拒答准确率均不得低于纯向量。
- vector：Recall@5、MRR@5、引用命中率、拒答准确率均为 83.33%。
- hybrid：Recall@5、MRR@5、引用命中率、拒答准确率均为 93.33%。
- 关键词 Recall@5：100% → 100%，达到指标上限；拒答分类 Recall@5：83.33% → 83.33%，修复了低覆盖率关键词候选造成的回归。
- 剩余 `refusal-03` 是纯向量已有的误召回；`multi-turn-06` 需要阶段 3D 使用历史改写，不在 3B 扩大修改范围。

## 3C 重排序验收结果（未通过）

- 本地报告：`backend/reports/stage3b-hybrid.json` 与 `backend/reports/stage3c-rerank.json`，均被 Git 忽略但保留在当前工作树。
- 固定条件：同一份 30 条 `stage3.jsonl`、同一评估知识库；3C 使用 `candidate_k=20`、`BAAI/bge-reranker-base`、CPU、严格禁止 fallback。
- 测量契约：rerank 模式只执行一次 embedding、hybrid candidate_k 检索和 BGE 重排；检索文件、Recall/MRR、引用均来自最终重排 Top5；延迟覆盖 embedding + hybrid candidate_k + reranker，不含回答生成。
- 3B hybrid：MRR@5 = 93.33%，引用命中率 = 93.33%，P50 = 15.20 ms，P95 = 18.12 ms。
- 3C rerank：MRR@5 = 93.33%，引用命中率 = 93.33%，P50 = 62.77 ms，P95 = 106.07 ms。
- 质量门：MRR@5 相对提升 0.00%，低于至少 5% 的要求；引用命中率持平，满足不下降要求。由于 MRR 门未通过，Task 5 与 3C 保持未完成，3D 阻塞。
- 根因证据：28 条案例在 3B 已经取得 RR=1，重排没有可提升的名次；`refusal-03` 只有一个误召回候选，当前重排只排序而不剔除；`multi-turn-06` 没有候选，重排无法恢复历史上下文缺失造成的漏召回。

## 3C.1 生产安全增强

- 接受门默认关闭；仅当显式配置有限的 `RAG_RERANKER_MIN_SCORE` 时过滤成功重排后的低分候选。BGE 原始分数不是概率，也不与向量相似度阈值共用。
- 独立校准报告：`backend/reports/stage3c1-reranker-calibration.json`（Git 忽略）。真实 `BAAI/bge-reranker-base` 在 CPU 上校准 24 条案例（12 正、12 负），约束为 FAR=0 且正样本接受率至少 80%；建议阈值为 `0.4118897467851639`，实测 FAR=0、正样本接受率=83.33%。
- 固定回归报告：`backend/reports/stage3c1-rerank-threshold.json`（Git 忽略）。仅在当前进程临时设置上述阈值，固定 30 条的 Recall@5、MRR@5、引用命中率、拒答准确率均为 93.33%，P50=62.16 ms、P95=101.21 ms；30/30 案例的 `accepted_chunk_count` 均等于最终 `retrieved_files` 数量。
- 安全语义：Provider 成功但低分时不 fallback；全部候选被拒绝时复用普通问答、流式问答和评估的安全拒答链路。
- 回归证据：非数据库测试 `275 passed / 60 skipped`；临时空库从零迁移至 Alembic head 后 `60 passed`，`finally` 删除并确认剩余数据库计数为 0；Ruff check 通过，143 个文件 format check 通过。
- 阶段结论：阈值评估 MRR@5 为 93.33%，与 3B hybrid 的 93.33% 相同，相对提升仍为 0.00%，未达到至少 5%。因此原 3C Task 5 继续未完成，3C 未通过，3D 继续阻塞。

## 继续执行时的最小提示

```text
按照 docs/阶段3执行进度.md 和当前阶段计划继续执行。
只完成当前未完成 Task。
使用计划标注的 Sol 或 Terra。
通过验证后更新执行进度，不提前执行下一阶段。
```

